# 前端环境变量示例
# 注意：不要在 VITE_API_BASE_URL 中包含 `/api` 后缀，以免造成重复路径（例如 http://localhost:8080/api/api/...）
VITE_API_BASE_URL=http://localhost:8080

# 生产环境部署时：
# - 若后端与前端同域部署（如通过 Nginx 反向代理到 /api），可以留空，使用相对路径
# - 若后端为跨域服务，则设置为后端域名（例如 https://api.your-domain.com）
# VITE_API_BASE_URL=
