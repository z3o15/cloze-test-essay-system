# 生产环境配置
VITE_APP_TITLE=完形填空学习系统
VITE_APP_VERSION=1.0.0

# API 配置 - 生产环境
# VITE_API_BASE_URL=http://localhost:8081  # 注释掉，使用Docker构建参数
VITE_API_TIMEOUT=30000

# 功能开关（不变）
VITE_ENABLE_MOCK=false
VITE_ENABLE_DEBUG=false

# 第三方服务配置（不变，后续替换为实际值即可）
VITE_TENCENT_SECRET_ID=your_secret_id
VITE_TENCENT_SECRET_KEY=your_secret_key

# 部署配置 - 核心修改：若前端部署在子路径，保留此配置；若在根路径，设为 /
VITE_PUBLIC_PATH=/